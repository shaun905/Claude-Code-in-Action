Write comprehensive tests for: $ARUGUEMENTS

Testing conventions:
- Use Vitests with React Testing Library
- Place test files in a **tests** directory in the same folder as the source file
- Name test files as [filename].test.ts(x)
- Use @/ prefix for imports

Coverage:

- Test happy path
- Test edge cases
- test error states
- Focus on testing behaviour and public API's rather than implementation details.